version: 2.1
jobs:
  build:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run: python -m pip install --upgrade pip
      - run: pip install -r GitHubApi567-hw4a/requirements.txt
      - run: pytest -q GitHubApi567-hw4a/tests
workflows:
  build_and_test:
    jobs:
      - build
